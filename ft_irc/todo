
Erreurs a check:
    - Regarder le password pour join.
    - regarder pour quand on join un channel qui a une autre syntax genre: mdr et Mdr -> doivent rejoindre le meme chan.

Commandes restantes:
    sever class:
    - definir le mot de pass pour pouvoir se co au serveur - done
    - parsing de PASS dans la class server - done
    - je pense - parsing de QUIT dans la class server - need la fonction pour send to all chans - done
    - parsing de LIST dans le server pour savoir tous les diffrents noms de channels class server - done a cleaner

Camille
    - comparaison de string - tout mettre en MAJ
    - au delq du premier join, pas de message de date de creation? Je n'arrive pas a comprendre comment fonctionne irssi a ce niveau, mais sur la doc ce nest pas obligatoire
    Ok jai trouve, cest quand on fait MODE #<nom du chan>
    que irssi nous envoie ca en commande et nous on lui rep la date du chan.

Elouan
    quand le dernier mec d'un chan fait /quit, il reste dans la base de donnee
    conditional jump quand on fait 'MODE #mdr' sans param supplementaire
    refaire l'ordre des channels quand on va les creer ( ordre alphabetique)

    other class:
    - parsing de PART 
    - parsing de MODE
    - parsing de KICK OK
    - parsing de TOPIC OK
    - parsing de NAMES ( bon la commande fonctionne toute seule )
    - parsing de WHOIS ( a voir si on fait )
    - parsing de WHO qui demande linfo de tous les utilisateur dun canal

    flag a gerer pour MODE:
        <nom du canal> on doit renvoyer les flag actif du canal. (renvoi toujours ca apres la creation du canal)
       -i +i : Le canal devient sur invitation uniquement.
       -t +t : Restreint la modification du topic aux opérateurs de canal.
       -k +k : Définit ou supprime une clé (mot de passe) pour accéder au canal.
       -o +o : Donne ou retire les privilèges d'opérateur de canal à un utilisateur.
       -l +l : Définit ou supprime la limite maximale d'utilisateurs pouvant rejoindre le canal.


commande pour se connecter directement a irssi avec les infos que lon veut

irssi -c 127.0.0.1 -p <port> -w <password> -n <nickname>

irssi -c localhost -p 6667 -w pass -n azbk


DCC transfert de fichier:

pour changer le repo de reception: ( sinon il va automatiquement dans /home/emauduit)

/set dcc_download_path <chemin_du_dossier>

dcc_download_path /home/emauduit/Documents/TEST

envoyer un fichier:

/dcc send <name> <chemin du fichier>

reception:

/dcc get <name du mec qui a envoye>

dcc en attente:

/dcc list


checks : 
- ifndef pas ds tous les hpp
- TOPIC : 
  - supprimer les ":" dans le message "azbk changed the topic of #test to: :retest" et "Topic for #test: :retest" qd join
- MODE : 
  - gerer l'envoi automatique de /mode #chan b

  maj sur part #CHAN
  sur list 